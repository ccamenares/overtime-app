<div class='container-fluid'>
	<h1>Hello <%= current_user.full_name %></h1>
	<div class="pending-homepage row">
	<h3>Items Pending Your Approval</h3>
	<hr>
		<%= render partial: 'pending_approval', locals: { pending_approvals: @pending_approvals } %>
	</div>

	<div class="pending-homepage">
		<h3>Confirmation Log</h3>
		<hr>
		<%= render partial: 'recent_audit_items', locals: { recent_audit_items: @recent_audit_items } %>
		<div class='clearfix'>
			
		</div>
		<%= link_to 'View All Items', audit_logs_path, class: 'btn btn-primary' %>
	</div>
</div>